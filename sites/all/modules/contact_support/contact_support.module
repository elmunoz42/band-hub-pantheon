<?php

function contact_form_alter(&$form, &$form_state, $form_id) {
    if ($form_id == 'contact_site_form') {
        $form['copy'] = array(
            '#type' => 'textfield',
            '#title' => t('It worked'),
            '#default_value' => t('Wuzzz up'),
        );
    }
}
